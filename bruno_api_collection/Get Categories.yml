info:
  name: Get Categories
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/categories"
  body:
    type: json
    data: |-
      {
        "name": "another_one",
        "description": "another boring item",
        "is_vegetarian": false,
        "available": false,
        "category": "yellow"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
