info:
  name: Create Menu
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/menu"
  body:
    type: json
    data: |-
      {
        "name": "chole bhature",
        "description": "test good",
        "is_vegetarian": true,
        "available": true,
        "category": "Punjabi"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
