info:
  name: Get Tags
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/tags"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
