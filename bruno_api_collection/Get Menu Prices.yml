info:
  name: Get Menu Prices
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/menu-price"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
